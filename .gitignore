# Python specific
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/                  # Your virtual environment
lib/
lib64/
local/
share/
include/
bin/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.pytest_cache/
htmlcov/
.nox/
.ipynb_checkpoints/    # Jupyter notebooks checkpoints

# Distribution / Packaging
build/
dist/
*.egg-info/
*.egg

# Editors / OS generated files
.idea/                          # JetBrains IDEs (PyCharm, IntelliJ, etc.)
.vscode/                        # VS Code
*.sublime-project
*.sublime-workspace
.DS_Store                       # macOS system files
Thumbs.db                       # Windows thumbnail cache
ehthumbs.db
.project                        # Eclipse project files
.classpath
.settings/

# Log files and run artifacts
*.log
*.log.*
logs/
tmp/
temp/
runs/                           # TensorBoard log directories (if used in this version)

# Model weights / Checkpoints and replay memory
model_weights/                  # Contains trained models (.pth) and replay memory (.pkl)
*.pth                           # Explicitly ignore PyTorch model files (if not in model_weights/)
*.pkl                           # Explicitly ignore pickle files (if not in model_weights/)
